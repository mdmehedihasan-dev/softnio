/* eslint-disable react/prop-types */
const Button = ({title}) => {
  return (
    <button className="text-[#0A1425] font-bold text-[16px] bg-[#FEBF00] py-2 px-6 uppercase ">
      {title}
    </button>
  );
};

export default Button;
